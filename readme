docker pull salamahs/v1
docker pull salamahs/db
cd compose
docker-compose up --build
# must now be running
# from browser enter 127.0.0.1:5000

# you can use the project file to make a new images without pulling the images from dockerhub
# by typing in the main directory : docker-compose up --build 
